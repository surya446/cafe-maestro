// Capacitor Cordova Android Plugins — stub module.
// Generated by sync-tv.js. No Cordova plugins are installed;
// this module exists only to satisfy the Gradle dependency
// declared in app/build.gradle and settings.gradle.
apply plugin: 'com.android.library'

android {
    namespace "com.capacitorjs.cordova.plugins"
    compileSdk rootProject.ext.compileSdkVersion

    defaultConfig {
        minSdkVersion rootProject.ext.minSdkVersion
        targetSdkVersion rootProject.ext.targetSdkVersion
    }
}

repositories {
    flatDir {
        dirs 'src/main/libs'
    }
}

dependencies {
}
